            MIN_VALUE / 10 && digit < -8)) {
                return 0; // Un
            }
            reversed = reversed * 10 + digit;
        }
        return reversed;
    }
    public static void main(String args[]) {
        int x = 123;
            10 || (reversed == Integer.
            if (reversed < Integer.MIN_VALUE / 
            MAX_VALUE / 10 && digit > 7)) {
                return 0; // Overflow case
            }