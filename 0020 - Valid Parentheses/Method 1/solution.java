                if(stack.isEmpty()) {
                    return false;
                 } else if((stack.peek()=='
                 (' && c==')') || (stack.peek()
                 =='[' && c==']') || (stack.peek
                 ()=='{' && c=='}')) {
                    stack.pop();
            if(c==')'  || c=='}' ||  c==']'  ) {
                stack.push(c);
            }
            if(c=='(' || c=='{' || c=='[') {
            char c = s.charAt(i);
        for(int i = 0; i<s.length(); i++) {
        Stack<Character> stack = new Stack<>();
    public boolean isValid(String s) {
class Solution {